let sub_btn = document.getElementById("sub")
let unsub_btn = document.getElementById("unsub")


const subscribe = () => {
   // todo:
   // 1. Check if the user already exist
   // 2. If not add to the db
}
const unsubscribe = () => {
    // todo:
    // 1. check if the user exist in db
    // 2. if yes unsubscibe from the list
}

sub_btn.addEventListener("click", subscribe);
unsub_btn.addEventListener("click", unsubscribe);